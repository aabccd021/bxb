/* istanbul ignore file */

import { FirebaseOptions } from 'masmott';

export const options: FirebaseOptions = { projectId: 'demo-2ch' };

export const spec = { spec: { thread: { src: {} }, reply: { src: {} } } };
