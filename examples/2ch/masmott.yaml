schema:
  thread:
    view:
      default:
        replyCount:
          type: count
          count: reply
          groupBy: threadId

  reply:
    data:
      threadId:
        type: refId
        referTo: thread
      text:
        type: string

firebase:
  projectId: demo-2ch
