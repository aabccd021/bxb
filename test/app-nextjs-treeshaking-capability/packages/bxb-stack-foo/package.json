{
  "name": "bxb-stack-foo",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && swc src --out-dir dist/cjs --source-maps --config module.type=commonjs && swc src --out-dir dist/es6 --source-maps && tsc --declaration --emitDeclarationOnly --declarationMap --sourceMap --skipLibCheck --outDir './dist/cjs'",
    "test": "tsc --noEmit"
  },
  "main": "dist/cjs/index.js",
  "module": "dist/es6/index.js",
  "types": "./dist/cjs/index.d.ts",
  "dependencies": {
    "bxb": "link:../../../.."
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.19",
    "typescript": "^4.9.3"
  }
}
