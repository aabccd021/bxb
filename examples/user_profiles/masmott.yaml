schema:
  profile:
    data:
      name:
        type: string
      birthPlace:
        type: string
      hobby:
        type: string
    view:
      card:
        select:
          name:
      page:
        select:
          name:
          birthPlace:

firebase:
  projectId: demo-profile-board
